[variables]
NIXPACKS_NODE_VERSION = "20"
# Runtime: defina DATABASE_URL no Railway (ex.: file:/data/eduhub.db com volume)
# e AUTH_SECRET com pelo menos 32 caracteres.

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
