[variables]
NIXPACKS_NODE_VERSION = "20"
# Runtime: DATABASE_URL do PostgreSQL Railway (${{Postgres.DATABASE_URL}}).

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
